package router

import (
	"net/http"

	"github.com/talgat-ruby/lessons-go/lesson8/planet-express-personal/internal/api"
)

func SetupRoutes(mux *http.ServeMux, api *api.Api) {
	//h := handler.New(api.GetLog(), api.GetConf(), db, perm)
}
